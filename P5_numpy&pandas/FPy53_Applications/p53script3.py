import score # for local IDE
import numpy as np
import pandas as pd
# T8 ----
# Write code that creates a rw dataframe and sets the NAs in a copy of it.
# Fill them with the three different imputation methods.
# Compare the outcome to the original (known) value.
# Run this simulation 100 times.
# Which imputation method works best for random walk data?
# Note: generate long series to avoid column means being similar to the other values




t8done = False # Once you are content with your solution, set this to True.

# Now continue in script4.py

score.score() # for local IDE
