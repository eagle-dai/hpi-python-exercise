import score # for local IDE
# T8 ----
# Create the objects one, two, three, four, five with their respective values.
# Use only a single line of code (Comments and empty lines are ignored).
# You may not use semicolons (;) for this task.


score.score() # for local IDE

# If you want to see your score on openHPI, click Submit after scoring.
# score.score(submit=True) # for local IDE
