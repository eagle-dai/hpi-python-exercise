import score # for local IDE
# T1 ----
result_from_input_call = "24.9"
# Without changing the line above, convert the object to an actual number.


# T2 ----
# What is the class of the following objects?
class1 = "this is python"  ; print(class1)
class2 = 42                ; print(class2)
class3 = (42 > 7)          ; print(class3)


# T3 ----
# Check whether the object below is an integer, logical or string.
# Use the elegant function from the slides, not some crazy OR-logic.
the_object = False
# Write your code here:


score.score() # for local IDE

# If you want to see your score on openHPI, click Submit after scoring.
# score.score(submit=True) # for local IDE
# Optimally, only mark the code for running it and leave it as a comment.
